spring:
  application:
    name: configserver
  main:
    banner-mode: off

  cloud:
    config:
      server:
        git:
          uri: https://github.com/prabinswain/EazyBank-ConfigServers
          force-pull: true



server:
  port: 8070